name: smartaquaapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: 0.66.0
  flutter_riverpod: ^3.0.3
  intl: ^0.20.2
  stylish_bottom_bar: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1  # ✅ Move here

flutter:
  uses-material-design: true
  assets:
    - assets/icons/ic_launcher.png

# ✅ Add this section at the root (NOT nested under flutter:)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/ic_launcher.png"
